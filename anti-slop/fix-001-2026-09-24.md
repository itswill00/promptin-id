# Perbaikan audit 001 — 2026-09-24

Item yang disetujui: 1, 2, 3, 4, 5. Semua dikerjakan.

1. Angka jujur: hero pakai hitungan dari data/prompts.json (20 total, 13 gratis, 7 pro). Klaim 200 dihapus dari metadata dan hero (app/page.tsx, app/layout.tsx).
2. Em dash: dihapus dari app/pro/page.tsx dan judul metadata. grep app/ dan components/ nihil.
3. Link Lynk.id placeholder diganti tombol disabled "Bayar via Lynk.id (segera)" + TODO di kode + catatan terlihat user (app/pro/page.tsx).
4. Tap target: chip kategori dan semua tombol/CTA min 44px (components/PromptGrid.tsx, app/pro/page.tsx).
5. Empty state: panel + tombol "Reset filter" saat hasil 0 (components/PromptGrid.tsx).

Verifikasi: tsc --noEmit 0 error. Build Next lokal tidak bisa di Termux Android (SWC 14.x tidak ada binary android-arm64), verifikasi build diserahkan ke Vercel.
